'use strict';

module.exports = function(definition) {
    let code = [
        `//------------------------------`,
        `// <auto-generated>`,
        `//     Generated using the swagen tool`,
        `// </auto-generated>`,
        `//------------------------------`,
    ];

    if (definition.metadata.title) {
        code.push(`// ${definition.metadata.title}`);
    }
    if (definition.metadata.description) {
        code.push(`// ${definition.metadata.description}`);
    }
    if (definition.metadata.baseUrl) {
        code.push(`// Base URL: ${definition.metadata.baseUrl}`);
    }

    code.push(
        ``,
        `import 'rxjs/Rx';`,
        `import { Observable } from 'rxjs/Observable';`,
        `import { Injectable, Inject, Optional, OpaqueToken } from '@angular/core';`,
        `import { Http, Headers, Response, RequestOptionsArgs } from '@angular/http';`,
        ``,
        `export const config = new OpaqueToken('Config');`,
        ``
    );

    return code;
}
